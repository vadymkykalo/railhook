// Placeholder for the dev server and the prerender. In the image nginx answers /config.js
// with the file the container writes at startup, and this copy is never served.
window.__RAILHOOK__ = { contactDomain: '' };
