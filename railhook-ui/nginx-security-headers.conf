# Re-included in every location with its own add_header: nginx doesn't inherit add_header then.
add_header X-Frame-Options "SAMEORIGIN" always;
include /etc/nginx/snippets/security-headers-common.conf;
