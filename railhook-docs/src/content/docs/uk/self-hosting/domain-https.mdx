---
title: Домен і HTTPS
description: Обслуговуйте Railhook на власному домені із сертифікатом, що оновлюється сам, або за проксі, який у вас уже є.
---
import { Steps, Tabs, TabItem, Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

Перш ніж відкривати інстанс в інтернет, дайте йому домен і HTTPS. Інсталятор може зробити це за вас, або ж можна поставити Railhook за наявний зворотний проксі.

<Tabs syncKey="https">
  <TabItem label="Вбудований HTTPS">
    <Steps>

    1. **Спрямуйте домен на сервер**

       Створіть для домену запис A з публічною IP-адресою сервера. Порти 80 і 443 мають бути вільні й відкриті в інтернет.

    2. **Встановіть із доменом**

       ```bash
       curl -fsSL https://railhook.io/install.sh | bash -s -- --domain hooks.example.com --email ops@example.com
       ```

    </Steps>

    ### Що змінює `--domain`

    | Налаштування | Значення |
    |---|---|
    | TLS-термінатор | Caddy на портах 80 і 443 сам отримує й оновлює сертифікат Let's Encrypt |
    | nginx панелі | Переходить на `127.0.0.1:8080`, за Caddy |
    | `APP_ENV` | `production`, тож платформа не запускається з небезпечною конфігурацією |
    | `APP_BASE_URL`, `CORS_ALLOWED_ORIGINS` | `https://<your domain>` |
    | Ingress-адреси джерел і адреси тунелів CLI | Беруться з `APP_BASE_URL`, тож провайдери отримують `https://<your domain>/ingress/…` |
    | `DB_SSL_MODE` | `require` |
    | `LOG_LEVEL` | `WARN` |
    | `WEBHOOK_TRUSTED_PROXIES` | `172.16.0.0/12`, щоб до журналу аудиту потрапляли справжні IP клієнтів |

    <Aside type="caution">
    Сертифікати зберігаються в томі `caddy_data`. Якщо його втратити, сертифікат буде видано знову під час наступного запуску, а Let's Encrypt обмежує частоту видачі.
    </Aside>
  </TabItem>
  <TabItem label="Власний проксі">
    <Steps>

    1. **Встановіть без домену, прив'язавши до loopback**

       ```bash
       curl -fsSL https://railhook.io/install.sh | bash -s -- --port 8080
       ```

       Потім задайте в `~/railhook/.env`:

       ```bash title="~/railhook/.env"
       RAILHOOK_BIND=127.0.0.1
       APP_ENV=production
       APP_BASE_URL=https://hooks.example.com
       CORS_ALLOWED_ORIGINS=https://hooks.example.com
       WEBHOOK_TRUSTED_PROXIES=172.16.0.0/12   # the address your proxy connects from
       ```

    2. **Проксуйте все на цей один порт**

       Надсилайте всі шляхи на `http://127.0.0.1:8080`. nginx усередині вже маршрутизує панель, `/api/`, `/ingress/`, `/hook/`, `/tunnel/` і `/ws/tunnel`. Шлях `/ws/tunnel` — це WebSocket, відкритий упродовж усієї сесії CLI, тож передавайте заголовки upgrade і задайте довгий тайм-аут читання (вбудований Caddy використовує одну годину).

    3. **Застосуйте**

       ```bash
       cd ~/railhook && ./railhook up -d && ./railhook doctor
       ```

    </Steps>
  </TabItem>
</Tabs>

## У режимі production

З `APP_ENV=production` API відмовляється запускатися, а не працює далі, якщо:

- секрет досі містить заглушку або типове значення з постачання,
- `WEBHOOK_ALLOW_PRIVATE_IPS=true`,
- `SWAGGER_ENABLED=true`,
- `CORS_ALLOWED_ORIGINS` досі містить `localhost`.

`./railhook doctor` перевіряє це заздалегідь. Він також попереджає, якщо `APP_BASE_URL` досі вказує на localhost або вимкнено `EMAIL_ENABLED`: без пошти облікові записи створюються вже підтвердженими, а запросити нікого не можна.

## Що далі

<CardGrid>
  <LinkCard title="Конфігурація" description="Пошта, секрети та решта налаштувань." href="/docs/uk/self-hosting/configuration/" />
  <LinkCard title="Оновлення й резервні копії" description="Тримайте інстанс актуальним і відновлюваним." href="/docs/uk/self-hosting/upgrade-backup/" />
</CardGrid>
