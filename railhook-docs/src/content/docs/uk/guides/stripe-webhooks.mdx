---
title: Як надійно отримувати вебхуки Stripe
description: Вебхуки Stripe через Railhook — перевірка підпису Stripe-Signature, пересилання кожної події у ваш сервіс із повторними спробами, дедуплікація за ідентифікатором події та відтворення після збою.
---
import { Steps, Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

У цьому посібнику Railhook стоїть між Stripe і вашим застосунком. Stripe надсилає вебхуки на джерело Railhook, а воно перевіряє підпис, зберігає кожну подію такою, якою та надійшла, і пересилає її у ваш сервіс. Якщо сервіс лежить, нічого не губиться: Railhook повторює пересилання, а те, що так і не вдалося доставити, можна відтворити пізніше.

## Чому повторних спроб самого Stripe недостатньо

Stripe повторює спроби. У live-режимі він до трьох днів намагається доставити невдалу подію з експоненційною затримкою; у пісочниці — тричі за кілька годин. Після цього подію можна повторно надіслати з Dashboard протягом 15 днів, а через Stripe CLI — протягом 30. Stripe також попереджає, що події можуть надходити не по порядку і що ендпоінт може отримати ту саму подію кілька разів. ([Stripe: Receive Stripe events in your webhook endpoint](https://docs.stripe.com/webhooks))

Що лишається на вас:

- **Ваш обробник має відповідати швидко.** Stripe просить повернути `2xx` ще до будь-якої складної логіки, інакше доставка завершиться тайм-аутом і вважатиметься невдалою. Railhook відповідає Stripe, щойно збереже подію, а ваш сервіс може обробляти її стільки, скільки дозволяє тайм-аут призначення.
- **Відновлення — по одній події.** Після тривалого збою повторне надсилання з Dashboard означає клацати подію за подією. Railhook відтворює всі збережені події за проміжок часу одним викликом.
- **Дублікати доходять до вашого коду.** Railhook розпізнає ідентифікатор `evt_…`: на повторну доставку тієї самої події він відповідає `202` зі збереженою копією і вдруге її не пересилає.

## Підключення Stripe

Знадобляться проєкт Railhook і API-ключ. Усе описане нижче можна зробити й у панелі керування, у розділі **«Вхідні»**.

```bash
export RAILHOOK_URL=https://railhook.io   # or your own instance
export RAILHOOK_API_KEY=...
export PROJECT_ID=...
```

<Steps>

1. **Створіть джерело.** Stripe показує секрет підпису лише після того, як ви зареєструєте URL, тож спершу створіть джерело без секрету. Із `verificationMode`: `PROVIDER` і без секрету воно відхиляє кожен запит із `401`, доки ви секрет не додасте, тож тим часом нічого неперевіреного не зберігається.

   ```bash
   curl -X POST "$RAILHOOK_URL/api/v1/projects/$PROJECT_ID/incoming-sources" \
     -H "X-API-Key: $RAILHOOK_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"name":"Stripe","providerType":"STRIPE","verificationMode":"PROVIDER"}'
   ```

   Збережіть `id` і `ingressUrl` із відповіді.

2. **Зареєструйте URL у Stripe.** У Workbench відкрийте **Webhooks** і натисніть **Create an event destination**. Виберіть **Your account**, позначте типи подій, які обробляєте, виберіть **Webhook endpoint** і вставте `ingressUrl` у поле **Endpoint URL**.

3. **Передайте Railhook секрет підпису.** На сторінці ендпоінта в Stripe натисніть **Reveal secret** і скопіюйте значення, що починається з `whsec_`. Додайте його в джерело:

   ```bash
   curl -X PUT "$RAILHOOK_URL/api/v1/projects/$PROJECT_ID/incoming-sources/$SOURCE_ID" \
     -H "X-API-Key: $RAILHOOK_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"name":"Stripe","hmacSecret":"whsec_..."}'
   ```

   Змінюються лише ті поля, які ви надіслали; секрет доступний лише для запису, і API ніколи його не повертає.

4. **Додайте призначення.** Це URL вашого сервісу, який має отримувати події:

   ```bash
   curl -X POST "$RAILHOOK_URL/api/v1/projects/$PROJECT_ID/incoming-sources/$SOURCE_ID/destinations" \
     -H "X-API-Key: $RAILHOOK_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"url":"https://api.example.com/webhooks/stripe","authType":"BEARER","authConfig":"{\"token\":\"...\"}","enabled":true}'
   ```

5. **Надішліть тестову подію** зі Stripe або виконайте `stripe trigger payment_intent.succeeded`. Вона з'явиться в розділі **«Вхідні» → «Отримані»** з одним пересиланням на кожне призначення.

</Steps>

## Як перевіряється підпис

Railhook читає `Stripe-Signature`, обчислює HMAC-SHA256 від `<t>.<raw body>` у hex із секретом `whsec_` і порівнює результат із кожним значенням `v1`. `t` має відрізнятися від часу на годиннику Railhook не більш ніж на 300 секунд. Поки ви ротуєте секрет у Stripe, той надсилає по одному `v1` на кожен активний секрет; запит проходить перевірку, якщо збігається будь-який із них, тож оновіть секрет у джерелі одразу після ротації. На запит, що не пройшов перевірку, Railhook відповідає `401` і не зберігає його, а Stripe повторює спробу.

## Що отримує ваш сервіс

Тіло точно таке, яким його надіслав Stripe, байт у байт, з оригінальним `Content-Type`. `Stripe-Signature` не пересилається: ваш сервіс автентифікує Railhook за обліковими даними призначення, тож секрет Stripe йому взагалі не потрібен. Кожен запит містить `Idempotency-Key`, однаковий для всіх спроб, і `X-Forward-Attempt`. Див. [Призначення](/docs/uk/incoming/destinations/#що-отримує-призначення).

Після `408`, `429`, будь-якого `5xx`, тайм-ауту чи помилки з'єднання Railhook повторює пересилання — усього до 5 спроб. Будь-який інший `4xx` одразу переносить його в **«Недоставлені пересилання»**, звідки його можна повторити, коли виправите сервіс.

## Локальна розробка з CLI

Спрямуйте призначення на свій комп'ютер через тунель, і тестові події Stripe, уже перевірені Railhook, дійдуть до коду, відкритого у вашому редакторі.

<Steps>

1. Встановіть CLI й увійдіть, як описано в [CLI](/docs/uk/tools/cli/), а потім відкрийте тунель до порту, який слухає ваш застосунок:

   ```bash
   railhook listen 3000
   ```

2. Додайте джерелу друге призначення: публічну адресу, яку вивела команда, з вашим маршрутом у кінці, наприклад `https://<host>/tunnel/<slug>/webhooks/stripe`. Шлях після адреси тунелю зберігається, тож запит потрапить на `http://localhost:3000/webhooks/stripe`.

3. Згенеруйте подію в пісочниці Stripe. Коли закриєте тунель, вимкніть або видаліть це призначення, щоб його пересилання не накопичувалися в «Недоставлених пересиланнях».

</Steps>

## Відтворення після збою

Якщо ваш сервіс лежав довше, ніж тривали повторні спроби, відтворіть те, що надійшло за цей час. Railhook створить нові пересилання на кожне призначення джерела, і кожне почне знову зі спроби 1:

```bash
curl -X POST "$RAILHOOK_URL/api/v1/projects/$PROJECT_ID/incoming-events/bulk-replay" \
  -H "X-API-Key: $RAILHOOK_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"sourceId":"'"$SOURCE_ID"'","from":"2026-09-18T10:00:00Z","to":"2026-09-18T14:00:00Z","verified":true}'
```

Обробник однаково має бути ідемпотентним за ідентифікатором події Stripe, бо відтворення надсилає й події, які ваш сервіс, можливо, уже обробив. Відтворити можна все, що ще зберігається: 7 днів у Railhook Cloud і скільки налаштуєте на власному сервері. Див. [Відтворення](/docs/uk/outgoing/replay/#відтворення-вхідних-вебхуків).

## Що далі

<CardGrid>
  <LinkCard title="Перевірка вхідних вебхуків" href="/docs/uk/incoming/verification/" description="Схема підпису кожного провайдера й захист від повторного використання підписів." />
  <LinkCard title="Призначення" href="/docs/uk/incoming/destinations/" description="Облікові дані, повторні спроби й трансформації для кожного призначення." />
  <LinkCard title="CLI" href="/docs/uk/tools/cli/" description="Тунелі, події й відтворення з термінала." />
</CardGrid>
